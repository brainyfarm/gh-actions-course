name: 04 - Using Actions
on:
    push:

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4            
            - name: Checkout Code
              run: |
                pwd
                ls -lR
            - name: Printing Folders
              run: ls -lR
        
